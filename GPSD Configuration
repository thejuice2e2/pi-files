# Edit the file /etc/default/gpsd to the below configuration.
# This will allow the gps data to be pulled in through USB0 utilizing the right socket as well.

# Default settings for gpsd.
# Please do not edit this file directly - use `dpkg-reconfigure gpsd' to
# change the options.
START_DAEMON="true"
GPSD_OPTIONS=""
DEVICES="/dev/ttyUSB0"
USBAUTO="true"
GPSD_SOCKET="/var/run/gpsd.sock"

# Once you edit the gpsd file you will need to make one change to the /lib/udev/gpsd.hotplug file.
# Need to add chmod a+rw $DEVNAME above the line gpsdctl $ACTION $DEVNAME

# This will allow the hotplug functionality of the gpsd daemon to work as it should.
